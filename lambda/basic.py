#lambda function is a small annoymous functions
#A lambda function can take any number of arguments, but can only have one expression.

a=lambda a,b: a*b
print(a(10,15))

a=lambda a,b,c: a+b*c
print(a(5,10,2))